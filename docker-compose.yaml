services:
  ministack:
    image: ministackorg/ministack:1.3.31
    ports:
      - "4566:4566"
    environment:
      - PERSIST_STATE=0
      - S3_PERSIST=0
      - REDIS_HOST=redis
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
