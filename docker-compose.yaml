services:
  # AWS
  floci:
    image: floci/floci:2.1.0-compat
    ports:
      - "4566:4566"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

  # # Azure
  # floci-az:
  #   image: floci/floci-az:0.13.0-compat
  #   ports:
  #     - "4577:4577"

  # # GCP
  # floci-gcp:
  #   image: floci/floci-gcp:0.9.0-compat
  #   ports:
  #     - "4588:4588"
